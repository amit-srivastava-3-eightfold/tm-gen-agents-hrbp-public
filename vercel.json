{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "node scripts/setup-git-auth.cjs && rm -rf node_modules/@tonyh-2-eightfold && npm cache clean --force 2>/dev/null; npm install --prefer-online",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
