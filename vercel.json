{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "node scripts/setup-git-auth.cjs && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
