{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "node scripts/setup-git-auth.cjs && rm -rf node_modules/@tonyh-2-eightfold && npm cache clean --force 2>/dev/null; npm install --prefer-online && echo '--- DS contents ---' && ls -la node_modules/@tonyh-2-eightfold/ef-design-system/ && echo '--- DS dist ---' && ls -la node_modules/@tonyh-2-eightfold/ef-design-system/dist/ 2>&1 || echo 'NO DIST DIR'",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
